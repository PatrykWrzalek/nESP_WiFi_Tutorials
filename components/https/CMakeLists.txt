idf_component_register(SRCS "https.c"
                       INCLUDE_DIRS "."
                       PRIV_INCLUDE_DIRS "."
                       REQUIRES main) # Dodanie komponentów nagłówka pliku źródłowego